#include <stdio.h>



int main(void) {

	char str[100] = "NULL";


	printf("문자열을 입력하세요 : "); // korea!
	scanf_s("%s", str, sizeof(str));

	printf("\n");
	printf("입력된 문자열은 \n\"%s\"\n입니다.", str);

	printf("입력된 문자열의 길이 : %d\n", strlen(str));

	return 0;
}
